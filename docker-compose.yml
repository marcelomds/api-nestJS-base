version: '3.8'

services:
  postgres:
    image: postgres:latest
    container_name: stock-system-db
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 12345678
      POSTGRES_DB: stock_system_db
